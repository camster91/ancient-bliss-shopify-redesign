# Spec: Collections Cleanup & Coming Soon Templates

1. **Collections Cleanup**:
   Modify `sections/collection-template-1.liquid` (or similar active collection template) to explicitly exclude products that have the tag `Bundle` or `Merch` from the main loop, ensuring "Our Products" only shows individual supplement bottles.
2. **Coming Soon Templates**:
   Create new JSON/liquid templates for `product.vitex-berry-new.liquid` and `product.meno-balance-new.liquid`. In these specific templates, replace the 'Add to Cart' button logic with a disabled "Coming Soon" button.
3. **New Product Template**:
   Create `product.saw-palmetto-new.liquid` as a standard product template.

Please write the code and exit.
